# Copy to .env and fill in:

# For AWS deployment:
VITE_API_URL=https://api.mystockify.in

# For local dev (or use .env.local with empty value to use Vite proxy):
# VITE_API_URL=http://localhost:8081
